import { Injectable } from '@nestjs/common';

@Injectable()
export class GeoService {
  constructor() // @InjectRepository(City, 'geo') private cityRepo: Repository<City>,
  // @InjectRepository(Country, 'geo') private countryRepo: Repository<Country>,
  {}
}
